# ── Geospatial ────────────────────────────────────────────────────────────────
# NOTE: GDAL is NOT pinned here — the Dockerfile installs it dynamically to
# match the system gdal-bin version (gdal-config --version). Pinning it here
# would clash with the system version and break the build.
earthengine-api==0.1.390
rasterio==1.3.10
geopandas==0.14.3
osmnx==1.9.3
shapely==2.0.4
# ── Deep learning ─────────────────────────────────────────────────────────────
# torch + torchvision installed separately in Dockerfile (CPU or CUDA wheel)
transformers==4.41.2
peft==0.11.1
bitsandbytes==0.43.1
accelerate==0.30.1
trl==0.9.4
datasets==2.19.1
sentencepiece==0.2.0
# ── Image processing ──────────────────────────────────────────────────────────
Pillow==10.3.0
numpy==1.26.4
opencv-python-headless==4.9.0.80
# ── Cloud storage ─────────────────────────────────────────────────────────────
google-cloud-storage==2.16.0
# ── API ───────────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
# ── Observability ─────────────────────────────────────────────────────────────
langfuse==2.36.1
# ── Utilities ─────────────────────────────────────────────────────────────────
requests==2.32.2
tqdm==4.66.4
